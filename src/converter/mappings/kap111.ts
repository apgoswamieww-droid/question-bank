import type { ConversionRule, VerifiedSample } from "../types";

/**
 * KAP111 — Unicode -> legacy mapping table.
 *
 * STATUS: NOT PROVIDED YET. See kap110.ts notes.
 */

export const KAP111_RULES: ConversionRule[] = [];

export const KAP111_SAMPLES: VerifiedSample[] = [];
