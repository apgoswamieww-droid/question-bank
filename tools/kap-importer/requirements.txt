# Optional dependencies for the KAP → Unicode importer.
#
# The core paste-to-Unicode tool needs NONE of these — it runs on the Python
# standard library alone. Install these only to switch on the extra capabilities:
#
#   pip install -r requirements.txt
#
# ── Phase 2: import questions straight from a PDF's text layer ──────────────
# pdfplumber pulls the raw KAP/ASCII bytes out of the PDF; the deterministic map
# then converts them exactly (no OCR, no AI).
pdfplumber>=0.11.0

# ── Phase 4: OCR for true scanned images (fully offline) ───────────────────
# Requires, in addition to these packages, the Tesseract engine and its Gujarati
# language data installed on the machine:
#   • macOS:   brew install tesseract tesseract-lang
#   • Ubuntu:  sudo apt-get install tesseract-ocr tesseract-ocr-guj
#   • Windows: install the UB-Mannheim Tesseract build, add the 'guj' language
# No image or text ever leaves the machine.
pytesseract>=0.3.10
Pillow>=10.0.0
